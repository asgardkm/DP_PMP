// my_math2.h

#ifndef MY_MATH_H // insuring macro is defined only once

#define MY_MATH_H
#define MIN(X,Y) (((X) < (Y))? (X) : (Y))
#define MAX(X,Y) (((X) > (Y))? (X) : (Y))
#define AVG(X,Y) (((X)+(Y)) / 2)
#define PRODUCT(X,Y) ((X)*(Y))
#endif
